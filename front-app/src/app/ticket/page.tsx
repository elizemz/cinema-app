import TicketPage from "@/components/app/ticketpage";
import React from "react";

const Page = () => {
  return (
    <div className="bg-gradient-to-t from-gray-500 to-gray-950 h-full">
      <TicketPage />
    </div>
  );
};

export default Page;
